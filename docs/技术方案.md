# 技术方案文档

## 一、技术栈总览

| 层 | 技术 | 版本 | 说明 |
|---|---|---|---|
| 后端框架 | Spring Boot | 3.2.x | JDK 17+ |
| 持久层 | MyBatis-Plus | 3.5.x | 简化 CRUD + 逻辑删除 |
| 数据库 | MySQL | 8.0 | utf8mb4 |
| 缓存 | Redis | 7.x | 登录 token、字典、热点数据 |
| 鉴权 | JWT (jjwt) | 0.12.x | 无状态登录 |
| 接口文档 | SpringDoc OpenAPI | 2.3.x | 替代 Swagger |
| 参数校验 | Hibernate Validator | 8.x | JSR-380 |
| 工具库 | Hutool | 5.8.x | 通用工具 |
| 构建 | Maven | 3.9+ | 多模块 |
| 前端框架 | Vue | 3.4+ | Composition API |
| 构建工具 | Vite | 5.x | 极速热更新 |
| UI 库 | Element Plus | 2.5+ | 蓝色系主题 |
| 状态管理 | Pinia | 2.x | |
| 路由 | Vue Router | 4.x | |
| HTTP | Axios | 1.x | 统一拦截 |
| 图表 | ECharts | 5.x | 数据看板 |
| 富文本 | WangEditor | 5.x | 知识库 |
| 权限 | v-permission 自定义指令 | - | 按钮级权限 |

## 二、后端目录结构（Maven 多模块）

```
backend/
├── pom.xml                      # 父 POM
├── crm-common/                  # 通用：常量、异常、工具、统一返回体
│   └── src/main/java/com/crm/common
├── crm-system/                  # 系统管理模块（用户/角色/菜单/部门/日志）
│   └── src/main/java/com/crm/system
├── crm-market/                  # 市场获客模块
│   └── src/main/java/com/crm/market
├── crm-customer/                # 客户管理模块
│   └── src/main/java/com/crm/customer
├── crm-business/                # 商机销售模块
│   └── src/main/java/com/crm/business
├── crm-service/                 # 售后模块
│   └── src/main/java/com/crm/service
├── crm-report/                  # 数据分析模块
│   └── src/main/java/com/crm/report
└── crm-admin/                   # 启动模块 + 全局配置
    ├── src/main/java/com/crm/CrmApplication.java
    └── src/main/resources/
        ├── application.yml
        ├── application-dev.yml
        └── mapper/             # MyBatis XML
```

### 模块间依赖规则
- `crm-admin` 依赖所有业务模块
- 业务模块（system/market/customer/business/service/report）互相之间**不直接依赖**
- 业务模块都依赖 `crm-common`
- 跨模块调用通过 `crm-admin` 的 facade 或事件总线，避免循环依赖

## 三、前端目录结构

```
frontend/
├── package.json
├── vite.config.ts
├── index.html
├── src/
│   ├── main.ts
│   ├── App.vue
│   ├── api/                     # 接口封装（按模块拆分）
│   │   ├── system.ts
│   │   ├── customer.ts
│   │   ├── opportunity.ts
│   │   └── request.ts          # axios 实例 + 拦截器
│   ├── assets/
│   ├── components/              # 通用组件
│   │   ├── Pagination.vue
│   │   ├── SearchForm.vue
│   │   └── ChartCard.vue
│   ├── layout/
│   │   ├── index.vue            # 主框架
│   │   ├── Sidebar.vue
│   │   ├── Header.vue
│   │   └── TagsView.vue
│   ├── router/
│   │   └── index.ts
│   ├── stores/                  # Pinia
│   │   ├── user.ts
│   │   ├── permission.ts
│   │   └── app.ts
│   ├── utils/
│   │   ├── auth.ts              # token
│   │   └── permission.ts        # v-permission 指令
│   └── views/
│       ├── login/
│       ├── dashboard/           # P1 首页工作台
│       ├── customer/            # P2 客户详情 + 列表
│       ├── clue/                # P3 线索池
│       ├── opportunity/         # 商机 + 漏斗
│       ├── contract/            # 合同 + 回款
│       ├── service/             # 工单
│       ├── report/              # P4 数据看板
│       └── system/              # 用户/角色/菜单/部门/日志
└── .env.development
```

## 四、关键设计

### 4.1 鉴权流程
1. 登录：`POST /auth/login` → 校验密码 → 生成 JWT（含 userId、deptId、roles）
2. 前端将 token 存 localStorage，axios 拦截器塞入 `Authorization: Bearer xxx`
3. 后端 `JwtAuthenticationFilter` 解析 token，写入 SecurityContext
4. 接口加 `@PreAuthorize("hasAuthority('customer:add')")` 控制权限

### 4.2 数据权限
基于 `sys_role.data_scope`，在 MyBatis-Plus 拦截器中拼接 SQL：
- 本人：`WHERE owner_id = #{userId}`
- 本部门：`WHERE owner_id IN (SELECT id FROM sys_user WHERE dept_id = #{deptId})`
- 本部门及下：递归 dept_id
- 全部：不加条件

### 4.3 线索自动分配
- 线索入库 → 写入 `mkt_clue_assign_log`
- 规则引擎按 区域/行业/销售能力 计算目标销售
- 调用企业微信 API 推送消息（1 分钟内）
- 异常兜底：分配失败 → 进入待分配池，管理员手动处理

### 4.4 公海池回收
Quartz 定时任务（每日 02:00）扫描 `cus_customer`：
```
WHERE in_pool = 0
  AND last_follow_time < NOW() - INTERVAL 30 DAY
```
批量 `UPDATE ... SET in_pool=1, owner_id=NULL`

### 4.5 离线同步（移动端）
- 前端使用 IndexedDB 缓存待提交数据
- 网络恢复后批量调用 `/api/sync/batch`
- 后端按 `client_uuid` 去重，保证幂等

## 五、第三方集成清单

| 系统 | 用途 | 接入方式 |
|---|---|---|
| 企业微信 | 消息推送、客户联系同步 | 服务端 API + CorpID/Secret |
| ERP | 订单/库存互通 | REST API 双向同步 |
| 财务软件 | 回款数据同步 | 定时拉取 + Webhook 回调 |
| 短信平台 | 验证码、提醒 | 阿里云短信 |
| OSS | 文件附件 | 阿里云 OSS / MinIO |

## 六、开发环境

| 工具 | 用途 |
|---|---|
| JDK 17 | 后端运行 |
| Maven 3.9+ | 后端构建 |
| Node.js 18+ | 前端构建 |
| MySQL 8.0 | 数据库 |
| Redis 7.x | 缓存 |
| IDEA / VS Code | IDE |
| Navicat | 数据库可视化 |
| Apifox | 接口调试 |

## 七、开发顺序建议

1. **第一阶段**：crm-common + crm-system（用户/角色/菜单/部门/登录）→ 前后端登录打通
2. **第二阶段**：crm-customer（客户 CRUD + 跟进 + 标签 + 公海池）
3. **第三阶段**：crm-market（线索 + 渠道 + 分配）
4. **第四阶段**：crm-business（商机 + 合同 + 回款）
5. **第五阶段**：crm-service（工单 + 售后记录）
6. **第六阶段**：crm-report（看板 + 报表）
7. **第七阶段**：移动端适配 + 离线
8. **第八阶段**：操作手册 + UI 精修
